@model helloserve.com.Web.Models.ProjectsViewModel

@{
    ViewBag.Title = "helloserve - Projects";
}

@foreach (var project in Model.Projects)
{
    <div class="content-block" style="height:50%">
        @Html.DisplayFor(m => project)
    </div>
}